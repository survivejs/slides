type Query {
  theme: Theme
  slides: [Slide]
}

type Theme {
  primaryColor: String!
  secondaryColor: String!
}

type Slide {
  layout: Layout!
  content: ContentType!
}

enum Layout {
  TITLE
  MARKDOWN
}

union ContentType = TitleContent | MarkdownContent

type TitleContent {
  title: String!
  author: String
}

type MarkdownContent {
  title: String!
  markup: String
}
